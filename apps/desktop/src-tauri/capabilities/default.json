{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the co_worker desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "co-worker:default"
  ]
}
