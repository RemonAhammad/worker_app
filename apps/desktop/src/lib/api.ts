/**
 * Re-exports the plugin's typed bindings under one local name so components
 * don't need to know which package they live in. If we ever swap the plugin
 * out, the rest of the app changes nothing.
 */
export * from 'tauri-plugin-co-worker'
